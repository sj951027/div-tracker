# DIV_TRACKER_KNOWLEDGE.md — 배당 적립 도우미 지식 문서

> 최종 갱신: 2026-08-19 · 이 문서가 "현재 상태"의 정본. **변경 이력은 `patch_note/` —
> 프로그램을 파악할 때는 이 문서와 patch_note/ 최신 노트를 반드시 함께 읽는다.**
> **개인 배당 적립 보조 도구** — 스크리너(dh-q7m3k)·us-screener와 완전 별개 프로젝트.
> 저 프로젝트들의 판정 체계(§11·사전등록·IC 검증)는 여기에 **이식하지 않는다** — 이 도구는
> 검증된 알파가 아니라 **합리적 휴리스틱 + 행동 규율 장치**다.

## §1. 정체성과 철학 (불변)

- 목적: 해외·국내 배당주/ETF를 **조금씩 모아가는(적립)** 것을 돕는다. 타이밍 도구가 아니다.
- **매수신호 아님 · 투자자문 아님** — 모든 화면·알림에 이 성격 유지.
- **"덜 보게 만드는" 방향으로만 개선한다.** 매일 보게 만드는 기능(일일 순위 푸시 등)은 금지.
  적립 투자의 최대 적은 고민(개입 유혹)이라는 전제가 이 도구의 설계 원점.
- 단순함이 기능이다: 파일 4개 + data.json + git. **DB 없음** — 모든 지표가 야후 이력에서
  매번 재계산 가능(재구성 가능 데이터는 쌓지 않는다. 스크리너 P0 교훈의 역방향 적용).
  git 커밋 이력이 공짜 시계열 보관소 역할.
- 스크리너에서 계승한 원칙: 근거 없는 매직넘버 금지(가중치는 동일가중 순위합·경계는 자연
  경계만), 표시 정직성(한계·경고 명시), 비치명 설계(실패해도 나머지는 돈다).

## §2. 순위 공식 (동결 — 변경 시 이 문서 갱신)

**순위 = rank(밴드% 내림차순) + rank(5일 총수익 오름차순)** — 시장(KR/US)별 동일가중 순위합.

- **밴드%("지금 싼가?")** = 현재 TTM 배당률이 그 종목 자신의 5년 분포에서 상위 몇 %인가.
  절대 배당률 순위는 배당 트랩(폭락 부실주) 랭킹이 되므로 쓰지 않는다(일드 밴드 방식).
- **5일 낙폭은 배당 포함(총수익) 기준** — 배당락(지급만큼의 기계적 하락)을 세일로 오인하는
  편향 제거(2026-08-15 수정). 월배당 상품 우대 편향도 함께 제거됨.
- **강등 가드**: 🚩 slump(5년 총수익 연평균<0 — 배당 받아도 자산이 준 종목) = 최하단(+2000),
  ⚠ nodata(분배금 데이터 없음 — 야후 커버리지 구멍) = 그 위(+1000). 경계 0은 자연 경계.
- 순위에 안 넣는 것(참고 컬럼): 배당률 절대값·배당변화·주기·5년 성적 크기·금리.
- 필터(전체/ETF/개별주)는 표시만 거름 — 순위는 시장 전체 기준 유지(의도).
- 백로그 B안: 밴드 단독 순위 + 낙폭은 타이브레이커 — "순위가 너무 촐랑댄다" 느껴지면 전환.

## §3. 표시 컬럼의 설계 의도

- **배당변화 1y%**: 밴드가 높아지는 두 경로(가격 하락 vs 배당 인상)를 구분하는 짝 컬럼.
  싼편+변화≈0 = 가격 하락형 세일 / 싼편+큰 플러스 = 배당 인상형(밸류업) / 큰 마이너스 = 삭감 주의.
- **※주기변경**: 최근 1년과 그 전 1년의 지급 주기 라벨이 다르면 표시 — 연→분기 전환기의
  배당변화 과대(좋게만 보이는 왜곡)를 화면에서 자백하게 하는 장치.
- **주기(월/분기/반기/연)**: 최근 365일 실지급 횟수로 매일 재판별(11+/3+/2/1회). 주기 변경은
  자동 추종, 특별배당 낀 해는 일시 오분류 가능(이듬해 자동 복구).
- **5y총수익(연평균)**: 배당 재투자 가정. 🚩의 근거 수치.
- **금리 컨텍스트(미10Y + 1개월 변화)**: 순위 무관 참고 한 줄. ±0.10%p 안쪽 "보합".
  금리보정 밴드는 **의도적으로 미채택** — 금리는 리스트 전체에 공통 작용이라 상대 순위에
  거의 무의미하고, 기준 둘이 싸우면 도구가 망가진다(2026-08-15 결정).

## §4. 알림 (텔레그램)

- **이벤트만 발송, 평소 침묵**: ① 밴드 80 신규 진입("싼편 진입") ② 🚩 신규 전환.
  일일 요약 푸시 금지(§1 철학). 첫 실행(직전 상태 없음)은 침묵.
- Secrets: TG_TOKEN·TG_CHAT (GitHub Actions) — **개인 챗**(양수 id) 사용, 공유방 금지.
  미설정 시 알림만 조용히 꺼짐(비치명).
- 백로그: 80 경계 진동 시 알림 반복 가능 — 소음 확인되면 히스테리시스(진입 82/해제 78) 도입.

## §5. 운영

- GitHub Actions 2회: **KR = UTC 11:30(KST 20:30, 월~금)** / **US = UTC 23:10(월~금)**.
  KR 시각은 KRX 시간외(18:00)·NXT 애프터마켓(20:00) 종료 후 — 야후가 세션 중 일부 펀드형
  종목의 이력을 잘라 반환하는 문제 회피(v1.4, 16:10에서 이동).
  US 시각은 서머타임 양쪽에서 마감+2~3시간이 되도록 고정(전환일 무대응 설계).
  시장 분리 갱신 — 다른 시장 행은 data.json에서 보존, updated에 시장별 시각 기록.
- 사용자 운영 루틴: 평소 안 봄(증권사 자동 정기매수) → 알림 오면 10분 판단 → 월 1회 배분
  점검 → 분기 1회 watchlist 정리(넣기보다 빼기·45/50 상한) → 연 1회 계좌 한도 점검.
- 계좌 전략(참고): 연금저축=국내상장 배당성장 ETF, ISA=월배당·커버드콜, 일반계좌=미국 직투.
  커버드콜 분배금은 ISA 궁합.

## §6. 데이터 소스와 한계 (정직성 고지)

- **야후(yfinance) 단일 의존이 유일한 구조적 약점.** 대비책: 호출 간 1.5s 슬립 + 30/90s
  백오프 재시도(Actions IP 429 실측 이슈), 전멸 시 기존 data.json 보존, 부분 실패는 fails 표시.
  실패 종목은 직전 행을 ⏸ stale로 보존해 표·알림 상태 유지(v1.2), 실패 사유는 로그에 구분
  출력(v1.3). 2026-08 실측: 장중·시간외 세션 중 펀드형 KR 7종의 가격 이력이 ~2개월로 잘려
  옴(밤 실행은 정상) → KR 실행을 KST 20:30으로 이동해 회피(v1.4).
- 한국 ETF 분배금 누락 잦음(실측) → ⚠ nodata로 분리 표시(부실이 아니라 미확인). 지속되면
  운용사 페이지 수동 확인 또는 종목 교체. (161510이 이 사유로 210780으로 교체됨)
- 금리 급변기엔 밴드를 보수적으로 읽기(5년 분포가 금리 체제 이동을 모름 — 화면에 명시).
- (구 16:10 실행의 EOD 지연 이슈는 20:30 이동으로 함께 해소 — v1.4.)
- 신규 상장(5년 미만) 종목은 밴드가 짧은 역사 기준.

## §7. 파일 구조

- `watchlist.csv` — 추적 종목(48개, 상한 ~50). ticker,market,name,type,memo. 수정은 이 파일만.
- `patch_note/` — 모든 변경의 패치노트(작성 규칙은 폴더 README.md). 코드·watchlist·설계·운영이
  바뀔 때마다 1건 작성 — 지식 문서가 "현재", patch_note가 "이력"을 담당.
- `fetch_rank.py` — 수집·계산·랭킹·알림. `--selftest`(네트워크 없이 산식 8종 검증),
  `--market KR|US|all|auto`(auto=UTC 시각 판별).
- `docs/index.html` + `docs/data.json` — Pages 표(필터·설명 박스·금리 줄 포함).
- `.github/workflows/daily.yml` — cron 2개 + workflow_dispatch(수동=all).

## §8. 결정 로그

- 2026-08-15: 최초 구축. 일드 밴드 방식 채택(절대 배당률 정렬의 트랩 회피). 고정 watchlist.
  시장 분리 실행. 총수익 가드(🚩)·nodata 가드(⚠) 도입. 배당변화·주기·※주기변경 컬럼.
  낙폭을 총수익 기준으로 교정(배당락 편향 제거). 금리보정 밴드 미채택(참고 한 줄만).
  이벤트 알림만 채택(일일 푸시 기각). DB 미도입 확정(재구성 가능 데이터).
  추가금 배분기·히스테리시스·B안 순위는 백로그(실운영 후 필요 확인 시).
- 2026-08-16: watchlist 45→48 — US 커버리지 공백 보완: VZ(통신)·MO(고배당 담배)·VYMI(미국외
  선진국 분산). VOO(저배당 지수형 — 밴드 신호 의미 약함)·초고배당 커버드콜 적층(철학 반대)·
  PFF(금리 미반영과 충돌)·SCHY(밴드 이력 부족) 등은 미채택 — 상세는 patch_note v1.1 참조.
  **patch_note 체계 도입**: 변경 이력을 patch_note/로 분리, 프로그램 파악 시 필독 규칙 명문화.
- 2026-08-18: KR 실행 7종목(맥쿼리인프라+국내 ETF 6) 수집 실패 실측(429 추정) — 실패 행이
  조용히 사라지고 복구 시 알림 침묵하는 문제 확인 → **실패 종목 직전 행 보존(⏸ stale) 채택**,
  화면에 흐린 행+날짜로 정직 표시. 상세·검증은 patch_note v1.2.
- 2026-08-19: v1.3 사유 로그로 원인 확정 — 429 아님(전일 "429 추정"은 이걸로 정정). 야후가
  장중·시간외 세션 중 펀드형 7종의 가격 이력을 6월말 이후로 잘라 반환(분배금은 정상, 밤 실행은
  전부 성공 — 실측). 대응: **KR 크론 16:10→20:30 이동(v1.4)**, 코드 무변경(auto 창 내 시각 선택).
